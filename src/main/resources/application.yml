spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
      hikari:
        username: sa
        jdbc-url: jdbc:h2:tcp://localhost/~/test;DB_CLOSE_ON_EXIT=FALSE
        idle-timeout: 600000
        minimum-idle: 2
      driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  devtools:
    livereload:
      enabled : true
    remote :
      restart:
        enabled : false
  thymeleaf:
    cache: false
jwt:
  issuer: wls990728@naver.com
  secretKey: study-springboot

